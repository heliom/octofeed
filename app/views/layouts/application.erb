<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @page_title || 'OctoFeed' %></title>
    <meta name="viewport" content="width=device-width,minimum-scale=1,maximum-scale=1">
    <meta name="description" content="Your GitHub News Feed, Without The Mess">
    <meta property="og:title" content="OctoFeed">
    <meta property="og:image" content="https://octofeed.heliom.ca/images/octofeed.png">
    <meta property="og:description" content="Your GitHub News Feed, Without The Mess">
    <%= stylesheet_include_tag 'styles' %>
  </head>
  <body>
    <header>
      <div class="container">
        <h1><a href="/">OctoFeed</a></h1>
        <div class="nav">
        <% if @user %>
            <div class="user">
              <%= gh_link @user[:username], :label => %(<img width="20" height="20" src="#{@user[:avatar]}" alt="#{@user[:username]}">) %>
              <%= gh_link @user[:username] %>
            </div>
            <a href="/logout" class="subtle">Log out</a>
        <% else %>
          <a href="/auth/github_private">Login</a></li>
        <% end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer>
      <div class="container">
        <p>Made by <a href="http://heliom.ca">Heliom</a>.</p>
        <p>
          Read the <a href="http://heliom.ca/en/blog/octofeed">blog post</a> or
          <a href="https://github.com/heliom/octofeed">fork</a>.
        </p>
      </div>
    </footer>
    <% if production? %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-31026266-5', 'heliom.ca');
        ga('send', 'pageview');

      </script>
    <% end %>
    <script>document.write('<script src=/js/lib/' + ('__proto__' in {} ? 'zepto-1.0rc1.min.js' : 'jquery-1.7.2.min.js') + '><\/script>')</script>
    <%= javascript_include_tag 'scripts' %>
  </body>
</html>
